%ul.nav.navbar-nav.pull-right
  %li.dropdown
    %a.dropdown-toggle{href: "#", role: "button", aria: { haspopup: "true", expanded: "false"}, data: { toggle: "dropdown"}}
      %img{src: "//www.gravatar.com/avatar/#{gravatar_hash(current_user.email)}"}
      %span.caret
    %ul.dropdown-menu
      %li
        %a{href: "#{url_for("/user/#{current_user.id}")}"}
          Profile
      %li
        %a{href: "#{url_for("/user/#{current_user.id}")}/logout"}
          Logout
